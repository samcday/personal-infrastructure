apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: synapse
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
  storageClassName: hcloud-volumes
